const registerValidation = require('../../users/helper/register_validator');
const loginValidation = require('../../users/helper/login_validator');

module.exports = {
  registerValidation,
  loginValidation
};
